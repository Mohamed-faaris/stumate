use simple shadcn comps do as little modification 
always use try to use shadcn components (use shadcn MCP)
use custom components in page but always place components in same folder as page
look is not important
do not modify routes if new route created use temp in route path like api/user-temp
use tanstack query for all